[tools]
rust = "1.98.0"

[tasks.build]
run = "cargo build"

[tasks.test]
run = "cargo test"

[tasks.release]
run = "cargo build --release"

[tasks.fmt]
run = "cargo fmt --check"

[tasks.clippy]
run = "cargo clippy --all-targets -- -D warnings"
